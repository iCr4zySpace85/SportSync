@model SportSync.Models.ViewModels.PageInfoViewModel

@{
    var idTorneo = ViewBag.IdTorneo;
}

<!-- Segundo navbar -->
@await Html.PartialAsync("~/Views/Shared/Partials/_navbarTorneo.cshtml")

<main class="p-5">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 p-3">
                <div class="shadow-lg bg-body rounded p-4">
                    <div class="input-group pb-4">
                        <strong>Resultados</strong>
                    </div>
                    <!-- Cards fila -->
                    <div class="row">
                        @foreach (var resultado in Model.EvaluacionesArbitraje)
                        {
                            <div class="col-md-6">
                                <div class="card">
                                    <div class="card-body card-partidos">
                                        <div class="image-container">
                                            @* <img src="https://via.placeholder.com/150" alt="Imagen del equipo @resultado.IdEquipoLocal" /> *@
                                            @* <strong>@resultado.NombreEquipoLocal</strong> *@
                                        </div>
                                        <div class="content">
                                            <p class="card-text fs-6 text-center">
                                                <span>@resultado.Ubicacion</span>
                                                @* <strong>@resultado.Resultado</strong><br /> *@
                                                <span>@resultado.Hora.ToString("HH:mm")</span>
                                                <span>@resultado.Fecha.ToString("MMMM dd")</span>
                                            </p>
                                        </div>
                                        <div class="image-container">
                                            @* <img src="https://via.placeholder.com/150" alt="Imagen del equipo @resultado.IdEquipoVisitante" /> *@
                                            @* <strong>@resultado.NombreEquipoVisitante</strong> *@
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                    <!-- Fin Cards -->
                </div>
            </div>

            <!-- Columna de noticias -->
            <div class="col-md-4 p-3">
                <!-- cards -->
                <div class="container-fluid p-3 border border-dark mb-3">
                    @foreach (var noticia in Model.Noticias)
                    {
                        <div class="card rounded-0 mb-3">
                            <div class="ratio ratio-16x9">
                                <iframe src="https://www.youtube.com/embed/DgdD5rUozew?si=O1REZp9SKbyK84uh" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
                            </div>
                            <div class="card-body">
                                <h5 class="card-title">@noticia.Titulo</h5>
                                <p class="card-text">
                                    @noticia.Contenido
                                </p>
                                <p class="card-text"><small class="text-muted">@noticia.FechaPublicacion.ToString("MMMM dd, yyyy")</small></p>
                            </div>
                        </div>
                    }
                </div>
                <!-- fin cards -->
            </div>
            <!-- fin Columna de noticias -->
        </div>
    </div>
</main>

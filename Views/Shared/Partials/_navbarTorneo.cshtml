@model SportSync.Models.ViewModels.PageInfoViewModel

@{
    var idTorneo = ViewBag.IdTorneo;
}

<nav class="navbar navbar-expand-lg navbar-custom p-0 border-bottom border-dark">
    <div class="container">
        <div class="navbar-brand" href="#">
            <img src="https://via.placeholder.com/60" alt="Torneo" />
            <strong>Nombre de torneo</strong>
        </div>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="@Url.Action("Torneo", "PageMain", new { idTorneo = idTorneo })">Equipos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Partidos", "PageMain", new { idTorneo = idTorneo })">Partidos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Resultados", "PageMain", new { idTorneo = idTorneo })">Resultados</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

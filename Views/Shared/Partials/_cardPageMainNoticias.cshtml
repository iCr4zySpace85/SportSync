@model SportSync.Models.ViewModels.PageInfoViewModel

<div class="col-md-4 p-2">
    <!-- cards -->
    @foreach (var noticia in Model.Noticias)
    {
        <div class="container-fluid p-3 border border-dark mb-3 rounded-2">
            <div class=" rounded-">
                <div class="p-3">
                    <p class="text-muted">@noticia.FechaPublicacion.ToShortDateString()</p>

                    <h5 class="card-title">@noticia.Titulo</h5>
                    <div class="row">
                        <p class="card-text text-break">@Html.Encode(noticia.Contenido)</p>
                    </div>
                </div>
            </div>
        </div>
    }
    <!--fin cards -->
</div>
